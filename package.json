{
  "name": "playitforward",
  "version": "0.0.1",
  "description": "Play It Forward Token - Million Acts of Kindness",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PlayItForward-IO/playitforward.git"
  },
  "keywords": [
    "Support",
    "Content",
    "Creators",
    "Token",
    "Micro-transactions"
  ],
  "author": "PlayItForward",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/PlayItForward-IO/playitforward/issues"
  },
  "homepage": "https://github.com/PlayItForward-IO/playitforward#readme",
  "scripts": {
    "truffle": "node_modules/.bin/truffle",
    "build": "yarn truffle compile",
    "migrate:mainnet": "yarn build && yarn truffle migrate --network mainnet",
    "verify:mainnet": "yarn build && yarn truffle run verify PlayItForward --network mainnet",
    "<---- Test Related Commands ---->": "test related commands",
    "test:dev": "yarn truffle test --network development test/*.js",
    "test:ci": "yarn build && yarn truffle test --network rinkeby test/pfwd.core.test.js",
    "test:ci:full": "yarn build && yarn truffle test --network rinkeby test/*.js",
    "<---- Maintenance Related Commands ---->": "maintenance related commands",
    "migrate:ci": "yarn build && yarn truffle migrate --network rinkeby",
    "deploy:ci": "yarn build && yarn truffle deploy --network rinkeby",
    "verify:ci": "yarn build && yarn truffle run verify PlayItForward --network rinkeby"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.2.0",
    "@openzeppelin/test-helpers": "^0.5.12",
    "@truffle/hdwallet-provider": "^1.4.1",
    "chai": "^4.3.4",
    "prettier": "^2.3.2",
    "truffle": "^5.4.1",
    "truffle-flattener": "^1.5.0",
    "truffle-plugin-verify": "^0.5.11"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
